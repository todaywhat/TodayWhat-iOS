git_url("git@github.com:todaywhat/TodayWhat-iOS-Certificate.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["baegteun.TodayWhat", "baegteun.TodayWhat.TodayWhatWidget", "baegteun.TodayWhat.watchkitapp"])
keychain_name("todaywhat.keychain")
# username(ENV["APPLE_ID"]) # Your Apple Developer Portal username

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
